# Provenance for contracts/squid -- a first-class fork, no longer vendored.
# Promoted from third-party/marketplace-squid on 2026-08-20; local changes are
# baked into this tree and evolve in place (the retired clone-and-patch series
# lives in git history at third-party/marketplace-squid/patches/).
#
# Parity sweeps diff against upstream from this rev forward.
url    = https://github.com/decentraland/marketplace-squid-core
branch = main
rev    = 065f5d2d4b0f2e0b8563736f16bc5b9c244897ea
#
# Baked-in changes on top of that rev:
# - ens-owner-is-beneficiary: handleNameRegistered recorded DCLControllerV2 as
#   ens.owner instead of the buyer; now uses _beneficiary. Rows indexed before
#   the fix keep the wrong owner until the ENS entities are re-indexed. If
#   upstream ships its own fix, reconcile by hand -- nothing auto-drops now.
# - drop-graphql-slack-sns: the GraphQL API server and Slack/SNS notify paths
#   are removed; catalyrst-market reads squid_marketplace directly.
# - upstream cherry-picks: a3781a0 (keep bulk indices), cf765f9 (cache v3 fee
#   config), c931f71 (empty-leg guards), portal auth.
# - 2026-09-04 sweep: 5bbb2bc (#112 index off-chain marketplace v3: V3 ABI
#   modules, OffChainMarketplace{,V2,V3} rename, shared Null constant), 84ebffe
#   (#116 ETH supportHotBlocks on + Portal retryAttempts: Infinity, the latter
#   first taken on the authed branch only and widened 2026-09-11 to the key-less
#   fail-open branch, which otherwise inherited portal-client's finite default of
#   20 attempts), 3d696da (#117 typeorm-store ^1.9.2 with batch-processor ^1.1.0,
#   evm-stream ^0.1.4, portal-client ^0.7.0; npmDepsHash in nix/squid.nix
#   regenerated with prefetch-npm-deps).
# - head-sync-status-qualified: head-notification.ts keeps the status table
#   schema-QUALIFIED, where upstream went unqualified in 1a65069 (#86) so the name
#   follows the promotion rename. We have no promotion path (upgrade.sh is never
#   invoked; the deployment creates squid_marketplace directly), so the rename cannot
#   happen and qualifying keeps the table isolated per deployment. The rest of
#   1a65069 IS ported (MAX_ATTEMPTS retry cap + the head-alert upsert, the latter
#   aliased because our table name is qualified).
# - 2026-09-06 sweep: 66f71f3 (#118 stop pinning search_path -- the deployment
#   schema now travels as SQUID_SCHEMA and DB_SCHEMA is unset, so typeorm-config
#   4.2.0 cannot pin a per-connection search_path; the head-notification hunk was
#   adapted to our schema-qualified status table, not taken verbatim).
#   ADAPTATION: upstream can read SQUID_SCHEMA alone because indexer.sh/restart.sh
#   always export it; our units exec the nix wrappers with an EnvironmentFile and
#   never run those scripts, so src/common/utils/deployment-schema.ts resolves
#   SQUID_SCHEMA with DB_SCHEMA as a DEPRECATED fallback. Drop the fallback once
#   the deployment's squid env sets SQUID_SCHEMA (reading DB_SCHEMA does not re-pin
#   search_path -- typeorm-config only pins when it is SET in the environment).
# - require-squid-schema (FORK-STRICTER than upstream, 2026-09-07): upstream's
#   src/{eth,polygon}/main.ts read process.env.SQUID_SCHEMA with no guard, because
#   indexer.sh/restart.sh always export a non-empty value before invoking node. Our
#   units exec the nix wrappers off an EnvironmentFile and never run those scripts, so
#   an unset var silently produced stateSchema `<chain>_processor_undefined` and a
#   re-index from genesis over live data. requireDeploymentSchema() in
#   src/common/utils/deployment-schema.ts now assertNotNull()s the resolved name at
#   entry-point module load; head-notification.ts deliberately keeps the permissive
#   deploymentSchema(). Both reads are trimmed, so an empty or whitespace-only value
#   normalizes to undefined, and a resolved name that is not a bare SQL identifier is
#   rejected -- upstream's indexer.sh only ever exports marketplace_squid_<timestamp>
#   or a resumed row value, so nothing upstream produces is rejected here, and the
#   qualified `"<schema>".head_sync_status` in head-notification.ts stays well formed.
#   Drop this once upstream grows its own guard or our env is guaranteed to set
#   SQUID_SCHEMA.
# - package-lock.json converted to lockfileVersion 3 (same resolved deps --
#   buildNpmPackage requires v3).
# - 2026-09-08 sweep: collections snapshots refreshed to upstream 66f71f3
#   byte-for-byte -- assets/collections_mainnet.json 69ddad3 (#108, block
#   80606688 -> 91689975) and 90fbed3 (#113, -> 92560518), assets/collections_amoy.json
#   2480206 (#97, block 10541238 -> 43349033, 40 -> 224 collections). Both files were
#   provably unmodified since the fork point, so the port is a straight copy; the old
#   address lists are strict subsets of the new ones and the unused "top" key that
#   upstream dropped was never read by src/polygon/utils/loaders.ts. The snapshot is a
#   warm-start filter, not a correctness gate: src/polygon/processor.ts address-filters
#   collection logs only up to collections.height and scans topic0-only past it.
#   The refresh does widen the address-filtered range (5772 -> 5902 addresses, up to
#   block 92560518), so it raises the urgency of provisioning SQD_PORTAL_API_KEY: that
#   filter is one Portal query, and the JSON text of its address array grows from 259741
#   bytes (5772 addresses, ~2.3 KiB UNDER the 262144-byte (256 KiB) Portal query cap
#   that src/common/utils/portal.ts documents; the same file describes the filter's
#   size as "~260 KB today and growing") to 265591 bytes
#   (5902 addresses, ~3.4 KiB OVER it). On the address-array measure this refresh is what
#   crosses the cap. The full Portal query also carries the other addLog blocks and the
#   fields spec, so a cold reindex may well have been over the cap before this port too;
#   either way SQD_PORTAL_API_KEY must be provisioned before any cold Polygon reindex,
#   and the address-filtered stretch the public endpoint cannot serve now runs to block
#   92560518. Reproduce with: node -e 'const m=require("./assets/collections_mainnet.json");
#   console.log(m.addresses.length,JSON.stringify(m.addresses).length)'
# - 2480206 (#97) and 846410f (#107) also ported for
#   src/polygon/tools/collectionsRetriever.ts, which still ran on
#   v2.archive.subsquid.io (key-gated since 2026-05-19, so every run died with
#   ArchiveCredentialsError) and resolved its chain twice: unset POLYGON_CHAIN_ID made
#   the tool pick the mainnet dataset, start blocks and file name while getAddresses()
#   -- which compares the same variable as a STRING -- handed it the AMOY factory
#   addresses, producing a snapshot whose height advanced by millions of blocks with an
#   unchanged address list. Committing that would make every collection created inside
#   the claimed range invisible to the processor. Now: chain resolved once and written
#   back to the environment, Portal DataSourceBuilder + run(), pretty-printed output, and
#   a fatal non-zero exit when a run scans more than 1M blocks and finds no collection.
#   ADAPTATION: the Portal URL comes from publicPortalDataset() in
#   src/common/utils/portal.ts (same host upstream hardcodes) rather than portalSource(),
#   because this one-shot CLI wants neither the shared endpoint's key nor its infinite
#   retry budget. #106's weekly refresh workflow is NOT ported -- this fork's
#   .github/workflows are inert here, so the snapshot is refreshed by hand.
# - 2026-09-08 sweep (fix round): ff9cc29 (#95) and 65e9b29 (#96) -- an order's
#   seller now comes from the EIP-712 signer, `seller: event._trade.signer` in
#   src/common/utils/offChainMarketplace.ts, instead of the payment leg's beneficiary.
#   The Shop signs listings whose payment beneficiary is the platform treasury and
#   credits the seller off-chain, so every such sale was recorded with the treasury as
#   sale.seller: the seller's own rows vanished from seller-filtered queries and
#   updateCreatorsSupportedSet credited the treasury rather than the creator. #96 is the
#   follow-up that narrowed #95 back to the order branch -- a bid is signed by its BUYER,
#   so no signed field names the seller and the bid branch keeps `sent[0].beneficiary`;
#   our bid branch already matched that net state, so only the order line changed. Only
#   newly indexed rows differ: with nothing redirecting the payment the signer and the
#   payment beneficiary are the same address, so a reindex reproduces historical rows
#   identically. Upstream's explanatory JSDoc on getTradeEventData is NOT carried (house
#   no-comment rule); the semantics are pinned by the ported sale-attribution cases in
#   src/common/utils/offChainMarketplace.test.ts, whose stale "this tree does not carry
#   #95/#96" caveat is now gone.
# - 2026-09-14 sweep: b81f7ee (#120 grant the reporting reader on each new squid
#   schema) ported into indexer.sh -- REPORTING_READER_USER="meltano_dapps_ro" plus
#   the two list appends (GRANT USAGE ON SCHEMA, and ALTER DEFAULT PRIVILEGES ...
#   GRANT SELECT ON TABLES). FIDELITY ONLY: indexer.sh is a vendored reference
#   script, kept text-synced to upstream (src/common/utils/schemaEnv.test.ts pins its
#   SQUID_SCHEMA lines) and never executed here -- the units exec the nix-wrapped
#   lib/*/main.js off an EnvironmentFile, and the deployment's bootstrap-squid.sh
#   creates the role/db/schema without the per-schema GRANT dance. meltano_dapps_ro is
#   upstream's BI reader and has no analog on this machine's clusters, so the port has
#   zero operational effect; it keeps the script diffable against upstream. The file
#   now matches upstream b81f7ee except for the explanatory `#` comments the house
#   no-comment rule strips.
# - portal-source-object-only (fork cleanup, 2026-09-14): PortalSource lost its dead
#   `| string` arm. portalSource() builds the object shape on both branches and its only
#   callers (src/{eth,polygon}/processor.ts .setPortal) consume that return value
#   directly; publicPortalDataset() returns a plain string and is typed as such.
#   Upstream has no named PortalSource type at all (portalSource() there returns an
#   inline object type), so there is no upstream shape to preserve.
# - portal-key-fail-open-loud (FORK-DIVERGENT from upstream, 2026-09-14): upstream's
#   portalSource() hard-requires the key -- `"x-api-key": assertNotNull(process.env.
#   SQD_PORTAL_API_KEY, ...)` -- so an unset key stops the boot. We keep the key-less
#   fallback to the unauthenticated public Portal, because the ETH filter is small
#   enough for the public endpoint and src/polygon/tools/collectionsRetriever.ts runs
#   key-less by design; adopting assertNotNull would break both. What was WEAKER than
#   upstream is now fixed: the fallback branch emits an error-level `[PORTAL]`
#   diagnostic naming SQD_PORTAL_API_KEY and the consequence, so a key-less Polygon
#   processor no longer retries a permanently-rejected `400 Query is too large` query
#   forever in silence (retryAttempts stays Infinity). The returned object is byte-
#   identical on both branches; src/common/utils/portal.test.ts pins the shape AND
#   that the diagnostic fires only on the key-less path. Drop this once the squid
#   unit env provisions SQD_PORTAL_API_KEY, after which assertNotNull could be
#   reconsidered for the processors only.
# - 2026-09-15 sweep: 4f013f4 (#124 strip NUL bytes from indexed strings before
#   persisting) ported verbatim -- stripNul() in src/common/utils/utils.ts (same
#   /\u0000/g regex and signature) plus all six upstream call sites: buildData() in
#   src/common/utils/data.ts (param csv -> rawCsv, `const csv = stripNul(rawCsv)` as
#   the first statement, BEFORE the charAt(0) version check, so a payload whose "0"
#   marker is preceded by a NUL is now accepted), ens.subdomain (handleNameRegistered),
#   estate.rawData in BOTH handleCreateEstate and handleUpdate, collection.name +
#   collection.symbol in handleAddItemV1, and parcel.rawData in handleUpdate. The
#   buildData(id, _data/data, ...) calls still pass the raw variable, as upstream does,
#   because buildData strips internally. Upstream's five-line rationale comment above
#   stripNul and its one-liner in buildData are NOT carried (house no-comment rule);
#   the semantics are pinned by src/common/utils/utils.test.ts, which mirrors upstream's
#   five stripNul cases and adds three fork-only buildData cases (upstream has none)
#   that pin the strip-before-validate ORDERING -- the part of the fix that actually
#   changes acceptance behavior. src/polygon/** is deliberately NOT stripped: upstream's
#   own 4f013f4 touches only common/utils/ and eth/handlers/ (7 files), and buildData is
#   called only from src/eth/handlers/{estate.ts:60,268, parcel.ts:39}, so the polygon
#   metadata writers get nothing for free -- see the polygon watch item below.
# - portal-diagnostic-dataset-aware (fork-only, 2026-09-15, closes S-portal2): the
#   `if (!apiKey)` branch of portalSource() now picks its consequence clause from
#   dataset.startsWith("polygon-"). The Polygon arm keeps the 256 KiB query-cap /
#   400 Query is too large text (PORTAL_DATASET is `polygon-mainnet` or
#   `polygon-amoy-testnet` in src/polygon/processor.ts:31); every other dataset
#   (`ethereum-mainnet` / `ethereum-sepolia`, src/eth/processor.ts:25) now names itself
#   and states the real consequence (public-tier rate limits, no quota guarantees)
#   instead of asserting a Polygon-only failure on the ETH path. Diagnostic text only:
#   the returned PortalSource is byte-identical on every path. portal.test.ts's seven
#   pre-existing assertions are unchanged and two cases were added.
# - polygon-nul-exposure (WATCH ITEM, opened 2026-09-15, deliberately NOT fixed): the
#   NUL-poisoning failure mode 4f013f4 closes on the ETH side is still open on Polygon.
#   src/polygon/modules/metadata/wearable/index.ts:38-39 and .../emote/index.ts:40-41
#   (name/description out of the tokenURI CSV) and src/polygon/modules/rarity.ts:21 /
#   src/polygon/handlers/collectionManager.ts:37,56 (rarity.name) write on-chain strings
#   into TEXT columns without stripNul, and buildData() -- the one shared writer that
#   does strip -- is called only from the two ETH handlers, so the common fix does not
#   reach them. Upstream 4f013f4 does not touch src/polygon/**, so porting would put us
#   AHEAD of upstream; left alone on purpose. Consequence if it fires: the batch write
#   throws `invalid byte sequence for encoding "UTF8": 0x00`, the batch transaction
#   aborts and the processor retries the same batch forever, wedging the Polygon indexer
#   at that block. This deploy does NOT unwedge an already-wedged Polygon processor. Take
#   the polygon hunks the moment upstream ships them; re-check src/polygon/** on the next
#   sweep.
# - 2026-09-17 sweep: dd72e10 (#125 make the amoy contract addresses match the logs
#   they are compared to) ported in full -- src/polygon/addresses/amoy.ts lowercases
#   CollectionFactory, Marketplace and OldCommittee (same values, case only; Subsquid
#   log addresses arrive lowercase and src/polygon/main.ts's batch-relevance check,
#   isMarketplaceV1 and the V1 fee-cut branch all compare with ===, so those three
#   comparisons were silently always false on Amoy and a batch whose only interesting
#   logs came from them was dropped); src/polygon/main.ts drops the dead
#   `log.address === addresses.ERC721Bid` disjunct (no Polygon address file exports
#   ERC721Bid -- Bid v1 is Ethereum-only -- so it compared against undefined on every
#   log, and BidV2 beside it already covers Polygon's only bid contract); and
#   src/common/utils/addresses.test.ts is taken VERBATIM (upstream's own file is
#   already node:test/node:assert, so no runner adaptation was needed, only the
#   house no-comment strip): it walks the four address modules -- arrays and plain
#   objects included, so polygon CreditsManager[] and eth collections{} are covered --
#   asserts every 0x-prefixed value matches /^0x[0-9a-f]{40}$/ naming the offending
#   path, and pins that the walk really descends past the top level.
# - 2026-09-17 sweep: 3a97f0d (#121 index the mainnet off-chain marketplace v3) ported
#   in full, all three behaviors. This also RESOLVES the standing DECISION item
#   trades-squid-core a993dd6 (#48): we are no longer fork-ahead on the mainnet V3
#   addresses, upstream fills the same two.
#   (1) FEE RESOLUTION BY EMITTING CONTRACT. New src/common/utils/feeCache.ts
#   (createFeeCache<T>(empty, merge) -> {begin,end,view,write,reset}) carries the
#   three-tier staging shared by both chains: committed map, a staged map for the open
#   batch, and one parked {fromBlock,toBlock,writes} slot promoted only when a later
#   batch starts past the parked batch's last block (the processor advances only past a
#   range it committed). begin() clears everything on a rollback into promoted history,
#   promotes on a clean advance, invalidates just the parked keys on a partial
#   re-delivery (a start INSIDE the parked range never replays the updates before it),
#   and drops the parked writes on a plain retry. Keys are lowercased on view/write.
#   ETH src/eth/state.ts gains offChainMarketplaceFeeCache plus
#   begin/endOffChainMarketplaceFeeBatch, resetOffChainMarketplaceContractData,
#   setOffChainMarketplaceFeeRate(marketplaceAddress, value) and
#   getOffChainMarketplaceFeeRate(ctx, block, marketplaceAddress), which seeds a cold
#   entry by reading feeRate() at block N-1 (state at N is post-block, so a rate update
#   later in N would otherwise price an earlier trade in it) and is deliberately NOT
#   try/caught. src/eth/handlers/marketplace.ts's handleTraded takes the emitting
#   address and drops its per-trade `new OffChainMarketplaceABI.Contract(...).feeRate()`
#   RPC read entirely. src/eth/main.ts begins the batch as the first statement of the
#   run callback, queues FeeRateUpdated in pass one, applies it in pass two at its
#   position in log order (before the Traded branch), passes log.address to
#   handleTraded, and ends the batch OUTSIDE the existing try/catch so begin and end
#   pair on every path. NOTE: upstream's commit body says a failed ETH fee read is
#   swallowed because the handler wraps its whole body in catch-and-log. That is not
#   true of upstream's file and is not true here: in src/eth/main.ts the only try in
#   the run callback opens AFTER the second-pass markteplaceEvents loop closes and
#   wraps the store writes only, so a throw out of getOffChainMarketplaceFeeRate
#   escapes the run callback and the batch fails and is retried -- the same semantics
#   as Polygon. endOffChainMarketplaceFeeBatch is therefore skipped on that path,
#   which is correct: the staged writes are dropped by the next begin().
#   POLYGON src/polygon/state.ts replaces the single shared
#   offChainMarketplaceContractData object with the same keyed cache;
#   getOffChainMarketplaceContractData(ctx, block, marketplaceAddress) reads the
#   contract at {...block, height: block.height - 1} against the EMITTING address (was:
#   the hardcoded V1 address at the trade's own block) and uses ??= per field so a value
#   already replayed in this batch is never overwritten by the chain read; the three
#   setters each take the marketplace address first. New src/polygon/utils/feeUpdates.ts
#   holds queueFeeUpdate/applyFeeUpdate (V2 and V3 share the same fee events, so one
#   wrapper serves every version). src/polygon/main.ts begins the batch first thing,
#   ends it on BOTH the early-return no-important-data path and the normal path, folds
#   the three previously-apply-on-sight fee cases into one queueing case (throwing
#   loudly if a topic reaches it that queueFeeUpdate does not know), applies them in
#   pass two in log order, and passes log.address to handleTraded.
#   src/polygon/handlers/marketplace.ts drops the `block.logs.find()` scan for the
#   first Traded log in the transaction and uses the emitting address it is now given:
#   THIS is the mint-attribution fix -- an aggregator settling one order on V2 and
#   another on V3 in one transaction used to attribute every mint in it to whichever
#   Traded log the linear scan found first. Both processors build
#   offChainMarketplaceAddresses = [V1,V2,V3].filter(a => a !== Null) and THROW when it
#   comes out empty, because an empty Subsquid `address` filter matches ANY address;
#   ETH subscribes to FeeRateUpdated only (its Sale has no collector column, so
#   FeeCollectorUpdated would be stream volume for a value nothing reads), Polygon keeps
#   all three fee events and widens the filter from the V1 address alone to all three.
#   (2) MAINNET V3 ADDRESSES FILLED: src/eth/addresses/mainnet.ts
#   OffChainMarketplaceV3 = 0x0f11d0d1671519683bd48abf3dbe779e300941cd (its now-unused
#   `Null` import is gone) and src/polygon/addresses/mainnet.ts
#   OffChainMarketplaceV3 = 0xe38ef22abe871513555cba89adfe45ab4f548ada (that file KEEPS
#   the Null import -- Bid = Null there). Until this landed the mainnet processors
#   matched no V3 log at all, so a mainnet V3 trade produced no Sale, no NFT owner
#   update and no primary-sale Issue accounting on either chain.
#   (3) FEE UPDATES APPLIED IN LOG ORDER, described under (1).
#   TESTS: upstream's three new suites are mirrored, not transplanted -- upstream moved
#   its suite to jest in 7836cc3 (#126) and ours stays on `node --test` over the
#   compiled lib (package.json:6, unchanged), so src/eth/state.test.ts,
#   src/polygon/state.test.ts and src/polygon/utils/feeUpdates.test.ts keep upstream's
#   describe/it structure and every case but use node:test + node:assert, and replace
#   jest.mock of the ABI module with a direct assignment to the module's `Contract`
#   export (tsc emits a lazy `Abi_1.Contract` property read at the construction site,
#   and node --test gives each test file its own process, so the swap is contained).
#   console.log is silenced with node:test's mock.method where the seed logs.
#   7836cc3 (#126 run the last two suites under jest) is NOT ported: test-tooling only,
#   and this fork's runner is unchanged. fc34c38 (#114) is a comment-only chore the
#   house no-comment rule drops either way.
#   REINDEX CONSEQUENCE (deploy-time, nothing here touches the deployment tree or nix/squid.nix):
#   filling the two mainnet V3 addresses and widening both fee-update log
#   subscriptions changes what the mainnet processors ingest GOING FORWARD only. A
#   running deployment will not backfill historical V3 trades or historical V2/V3
#   fee-rate history without a cold reindex from each contract's deployment block, and
#   a cold Polygon reindex still needs SQD_PORTAL_API_KEY provisioned first (the
#   address-filtered Portal query is already over the unauthenticated 256 KiB cap).
