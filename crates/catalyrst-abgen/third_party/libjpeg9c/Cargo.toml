[package]
name = "libjpeg9c"
version = "0.1.0"
edition = "2021"
license = "IJG"
description = "Vendored IJG libjpeg 9c decoder (FreeImage 3.18.0's bundled JPEG codec) with a thin RGBA decode wrapper"

[lib]
path = "lib.rs"

[build-dependencies]
cc = "1"
