//! HTTP request/response plumbing — params parser, pagination, error wrapper.
//! Direct port of `marketplace-server/src/logic/http/*.ts`.

pub mod errors;
pub mod pagination;
pub mod params;
pub mod response;
